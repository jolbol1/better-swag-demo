# Load Generator

The load generator creates simulated traffic to the demo.

## Accessing the Load Generator

You can access the load generator status page at `http://localhost:8080/loadgen/`.

## Modifying the Load Generator

The browser funnel is implemented with Artillery + Playwright.
See the [Artillery Playwright documentation](https://www.artillery.io/docs/playwright)
for the browser runtime and scenario model.
